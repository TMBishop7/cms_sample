$('#inner_<%= dom_id(@place) %>').html(
	'<%= escape_javascript(render "form") %></div>' +
	'<a class="kill" href="#">&times;</a>');

$('.kill', $('#inner_<%= dom_id(@place) %>')).click(function() {
	$.when( setFalse() ).done(function(){
		$('#inner_<%= dom_id(@place) %>').empty();
	});
});