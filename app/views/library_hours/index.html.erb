<h1>Listing library_hours</h1>

<table>
  <tr>
    <th>Day</th>
    <th>Open time</th>
    <th>Close time</th>
    <th>Library location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @library_hours.each do |library_hour| %>
  <tr>
    <td><%= library_hour.day %></td>
    <td><%= library_hour.open_time %></td>
    <td><%= library_hour.close_time %></td>
    <td><%= library_hour.library_location_id %></td>
    <td><%= link_to 'Show', library_location_library_hour_path(@library_location, library_hour) %></td>
    <td><%= link_to 'Edit', edit_library_location_library_hour_path(@library_location, library_hour) %></td>
    <td><%= link_to 'Destroy', [@library_location, library_hour], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Library hour', new_library_location_library_hour_path(@library_location) %>