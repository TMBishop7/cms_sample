$('#feedsEvents').prepend('<%= escape_javascript(render(@eventsfeed)) %>');
$('#feedsEvents > li').first().effect('highlight', {color: '#e1e1ff'}, 1500);
$('#eventsfeed_form > form')[0].reset();
createPicker();
var t = parseFloat($('#count').html());
var c = t+1;
$('#count').html(c);