$('#inner_<%= dom_id(@emergency_contact) %>').html(
	'<%= escape_javascript(render "form") %></div>' +
	'<a class="kill" href="#">&times;</a>');

$('.kill', $('#inner_<%= dom_id(@emergency_contact) %>')).click(function() {
	$('#inner_<%= dom_id(@emergency_contact) %>').html("");
});