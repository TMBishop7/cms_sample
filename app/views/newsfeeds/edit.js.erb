$('#inner_<%= dom_id(@newsfeed) %>').html(
	'<%= escape_javascript(render "form") %></div>' +
	'<a class="kill" href="#">&times;</a>');

$('.kill', $('#inner_<%= dom_id(@newsfeed) %>')).click(function() {
	$('#inner_<%= dom_id(@newsfeed) %>').html("");
});