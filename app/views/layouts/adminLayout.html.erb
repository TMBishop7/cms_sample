<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>iCampus Live</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <style type="text/css">
      body {
        padding: 15px 10px 40px;
      }
      .sidebar-nav {
        padding: 9px 0;
      }
  </style>
</head>
<body>
    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span12">
          <%= link_to raw('<div class="icampuslive-logo">Live</div>'), users_path %>
          <div class="flash-top">
            <% flash.each do |name, msg| %>
              <div class ="alert alert-<%= name %>">
                <%= msg %>
                <button type="button" class="close" data-dismiss="alert">&times;</button>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <div class="container-fluid">

      <div class="navbar">
        <div class="navbar-inner">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Admin Dashboard</a>
          <div class="nav-collapse collapse">
            <% if current_user %>
              <ul class="nav pull-right logg">
                <li class="dropdown">
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.name %> &nbsp;<b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to raw('<i class="icon-user icon-blue"></i> Log Out'), logout_path %></li>
                  </ul>
                </li>
              </ul>
            <% else %>
              <p class="navbar-text pull-right logg">
                <%= link_to "Log In", login_path, :class => 'navbar-link' %>
              </p>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>

    </div>
    <div class="container-fluid">
      <div class="row-fluid laywrap">
            <%= yield %>
      </div>
    </div><!--/.fluid-container-->
</body>
</html>